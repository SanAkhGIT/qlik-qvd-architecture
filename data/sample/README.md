# Sample Source Data

These CSVs are intentionally small and human-readable so the Qlik scripts can be tested from a clean checkout.

For a larger deterministic dataset, run `python python/generate_data.py`. The generator recreates the files with a fixed random seed.

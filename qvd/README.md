# QVD Outputs

The `raw`, `transform` and `semantic` directories are output locations for generated QVDs. Binary QVD files are intentionally ignored by Git; source scripts and sample data are version-controlled instead.
